{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://impresso.github.io/impresso-keyphrase/schemas/keyphrase/per-language-record.schema.json",
  "title": "Keyphrase per-language record",
  "description": "A non-ad article record exported into a language-specific JSONL file before keyword generation.",
  "type": "object",
  "required": ["id", "lg", "tp", "ft", "ad_class"],
  "properties": {
    "id": {
      "type": "string",
      "description": "Impresso content item identifier.",
      "pattern": "^[A-Za-z][A-Za-z0-9_]*-[0-9]{4}-[0-9]{2}-[0-9]{2}-[a-z]{1,3}-i[0-9]{4}$"
    },
    "lg": {
      "type": "string",
      "description": "ISO language code of this per-language output file.",
      "pattern": "^[a-z]{2,3}$"
    },
    "tp": {
      "type": ["string", "null"],
      "description": "Content item type from the upstream corpus."
    },
    "len": {
      "type": "integer",
      "description": "Text length reported by the upstream corpus or compiler.",
      "minimum": 0
    },
    "title": {
      "type": ["string", "null"],
      "description": "Article title when available."
    },
    "ft": {
      "type": "string",
      "description": "Full text that will be sent to the keyword-generation model.",
      "minLength": 1
    },
    "ad_class": {
      "type": "string",
      "description": "Impresso ad-classifier decision retained for traceability.",
      "const": "non-ad"
    }
  },
  "additionalProperties": true
}
