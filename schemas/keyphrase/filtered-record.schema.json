{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://impresso.github.io/impresso-keyphrase/schemas/keyphrase/filtered-record.schema.json",
  "title": "Keyphrase filtered record",
  "description": "A corpus record after the first keyphrase filter stage. The schema keeps the source record flexible while requiring the Impresso identifier and content type used downstream.",
  "type": "object",
  "required": ["id", "tp"],
  "properties": {
    "id": {
      "type": "string",
      "description": "Impresso content item identifier.",
      "pattern": "^[A-Za-z][A-Za-z0-9_]*-[0-9]{4}-[0-9]{2}-[0-9]{2}-[a-z]{1,3}-i[0-9]{4}$"
    },
    "tp": {
      "type": ["string", "null"],
      "description": "Content item type from the upstream corpus, usually article for retained keyphrase candidates."
    },
    "lg": {
      "type": ["string", "null"],
      "description": "ISO language code assigned upstream.",
      "pattern": "^[a-z]{2,3}$"
    },
    "len": {
      "type": "integer",
      "description": "Text length reported by the upstream corpus.",
      "minimum": 0
    },
    "ocrqa": {
      "type": "number",
      "description": "OCR quality score reported upstream.",
      "minimum": 0
    },
    "ft": {
      "type": "string",
      "description": "Full text when it is present in the filtered input."
    }
  },
  "additionalProperties": true
}
